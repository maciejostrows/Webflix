{% extends 'base.html.twig' %}

{% block body %}
    <h1>Movie creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('movies_index') }}">Back to the list</a><br>
            <a href="{{ path('main_default_adminpanel') }}">Powrót do panelu Administratora</a>
        </li>
    </ul>
{% endblock %}
