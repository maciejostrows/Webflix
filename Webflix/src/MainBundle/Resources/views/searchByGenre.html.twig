<html>
<body>

{% include 'MainBundle::header.html.twig' %}

{{ form(form) }}

{% for movie in result %}
    <a href="{{ path('main_default_movieshow', {id:movie.id}) }}">{{ movie.title }}</a><br>
{% endfor%}
</body>
</html>