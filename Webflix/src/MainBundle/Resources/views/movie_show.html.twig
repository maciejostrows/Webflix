
{% extends '::base.html.twig' %}

{% block body %}
{% include 'MainBundle::header.html.twig' %}

<h1>{{ result.title }}</h1><br>
Ocena: {{ average|number_format(2) }} Ilość ocen: {{ rows }} || <a class="btn btn-primary" href="{{ path('main_default_addrating', {id:result.id}) }}">Oceń film</a> || <a class="btn btn-success" href="{{ path('main_default_addtofavourites', {'id':result.id}) }}">Dodaj do ulubionych</a><br>

    Opis:<br>
{{ result.description }}<hr>
    <center>{{ result.path|raw }}</center><br>


<h2>Komentarze: </h2><br>

{{ form(form) }}







{% for comment in result.comments %}
    <h3>Autor: {{ comment.author }}</h3>   <a href="{{ path('main_default_addbadcomment', {'id':comment.id}) }}">Zgłoś komentarz</a><br>
{{ comment.text }}<hr>
{% endfor %}
{% endblock %}
